
	var person = {
  name: 'The Fastest Footracer',
  age: 28,
	};

	var person2 = {
		name: 'The Second Fastest Footracer'
		age: 22,
	};

	var person3 = {
		name: 'The Underdog'
		age: 20,
	};

	var race = {
  	capacity: 1600,
  	speed: {
    	max: 30,
    	min: 10,
  	},
	};

module.exports.whoWonTheRace = function() {
console.log(person[0]);
var result = person[0]);
};
return result;


/**
 * Run a race!
 * @param {object} the race set to run.
 * @param {object} settings - Pace at which to run
 * @param {function} cb - Called when each 400 meter lap is run. 4 total laps will be run
 */
